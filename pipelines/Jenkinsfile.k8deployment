pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        script {
            sh "echo test"
            def now = new Date()
            def time = now.format("yyMMdd.HHmm", TimeZone.getTimeZone('UTC'))
            println ${time}
        }
      }
    }
  }
}
