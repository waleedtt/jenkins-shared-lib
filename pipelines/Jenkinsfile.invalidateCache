@Library('sharedLibrary@master') _


pipeline {
    agent any

    environment {
        TEST     = credentials('temp-60')
    }


    stages {
        stage('test') {
            steps {
                sh 'echo $TEST'
                
                script{
                echo "teest"
                }
            }
        }
    }   
}