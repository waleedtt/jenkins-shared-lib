@Library('sharedLibrary@master') _


pipeline {
    agent any

    environment {
        TEST     = credentials('temp-60')
    }


    stages {
        stage('test') {
            steps {
                sh 'echo $TEST > /tmp/test'

                script{
                echo "teest"
                }
            }
        }
    }   
}