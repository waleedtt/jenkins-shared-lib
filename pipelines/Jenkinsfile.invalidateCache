@Library('sharedLibrary@master') _


pipeline {
    agent any

    environment {
        CACHE_BINARY_GCS     = credentials('temp-60')
    }


    stages {
        stage('test') {
            steps {
                // export GOOGLE_APPLICATION_CREDENTIALS=
                sh 'echo $TEST'

                script{
                    echo "teest"
                    ls -la
                }
            }
        }
    }   
}